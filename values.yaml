expose:
  config:
    domain: dev.nuxeo.com
    exposer: Ingress
    http: true
    tlsacme: true
    tlsSecretName: $NAMESPACE-tls
    urltemplate: '"{{.Service}}.{{.Namespace}}.{{.Domain}}"'
gcactivities:
  role:
    enabled: true
    rules:
    - apiGroups:
      - jenkins.io
      resources:
      - pipelineactivities
      verbs:
      - list
      - delete
    - apiGroups:
      - jenkins.io
      resources:
      - environments
      verbs:
      - get
      - list
      - patch
      - update
      - watch
    - apiGroups:
      - jenkins.io
      resources:
      - plugins
      verbs:
      - get
      - list
      - watch
    - apiGroups:
      - ""
      resources:
      - secrets
      - services
      verbs:
      - get
      - list
    - apiGroups:
      - apps
      resources:
      - deployments
      verbs:
      - get
    - apiGroups:
      - tekton.dev
      resources:
      - pipelineruns
      verbs:
      - get
      - list
      - watch
      - delete
jenkins:
  Master:
    Image: $INTERNAL_DOCKER_REGISTRY/nuxeo/$NAMESPACE-jenkinsx
    ImageTag: 0.0.33
  Persistence:
    volumes:
      - name: jenkins-secrets
        secret:
          secretName: jenkins-secrets
    mounts:
      - name: jenkins-secrets
        readOnly: true
        mountPath: /run/secrets/jenkins
  Servers:
    Global:
      EnvVars:
        ARENDER_DOCKER_REGISTRY: docker-arender.packages.nuxeo.com
        DOCKER_REGISTRY: docker.$NAMESPACE.dev.nuxeo.com
        DRY_RUN: $DRY_RUN
        PRIVATE_DOCKER_REGISTRY: docker-private.packages.nuxeo.com
        PUBLIC_DOCKER_REGISTRY: docker.packages.nuxeo.com
        YUM_REGISTRY: http://nexus.platform/repository/yum-registry/
  Agent:
    PodTemplates:
      # fix jx create devpod -l jx-base
      JX-base:
        DevPodPorts: 9999
        Containers:
          JX-base:
            LimitCpu: "1"
      Nuxeo-Package-10:
        Name: nuxeo-package-10
        Label: jenkins-nuxeo-package-10
        DevPodPorts: 5005, 8080
        volumes:
          - type: Secret
            secretName: jenkins-maven-settings
            mountPath: /root/.m2/
          - type: Secret
            secretName: jenkins-docker-cfg
            mountPath: /home/jenkins/.docker
          - type: Secret
            secretName: jenkins-release-gpg
            mountPath: /home/jenkins/.gnupg
        EnvVars:
          JENKINS_URL: http://jenkins:8080
          JAVA_TOOL_OPTIONS: "-Dsun.zip.disableMemoryMapping=true -XX:+UseParallelGC -XX:MinHeapFreeRatio=5 -XX:MaxHeapFreeRatio=10 -XX:GCTimeRatio=4 -XX:AdaptiveSizePolicyWeight=90"
          GIT_COMMITTER_EMAIL: platform+jx-bot@nuxeo.com
          GIT_AUTHOR_EMAIL: platform+jx-bot@nuxeo.com
          GIT_AUTHOR_NAME: nuxeo-platform-jx-bot
          GIT_COMMITTER_NAME: nuxeo-platform-jx-bot
          XDG_CONFIG_HOME: /home/jenkins
          DOCKER_CONFIG: /home/jenkins/.docker/
          # lets disable the noisy download artifact logging messages
          MAVEN_OPTS: -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=warn
        ServiceAccount: jenkins
        ImagePullSecret: kubernetes-docker-cfg
        Containers:
          Jnlp:
            Image: jenkinsci/jnlp-slave:3.26-1-alpine
            RequestCpu: "100m"
            RequestMemory: "128Mi"
            Args: "${computer.jnlpmac} ${computer.name}"
          Maven:
            Image: $DOCKER_REGISTRY/nuxeo/builder-java8:latest
            Privileged: true
            RequestCpu: "1"
            RequestMemory: "512Mi"
            LimitCpu: "2"
            LimitMemory: "1Gi"
            Tty: true
      Nuxeo-Platform-11:
        Name: nuxeo-platform-11
        Label: jenkins-nuxeo-platform-11
        DevPodPorts: 5005, 8080
        volumes:
          - type: Secret
            secretName: jenkins-maven-settings
            mountPath: /root/.m2/
          - type: Secret
            secretName: jenkins-docker-cfg
            mountPath: /home/jenkins/.docker
          - type: Secret
            secretName: jenkins-release-gpg
            mountPath: /home/jenkins/.gnupg
          - type: EmptyDir
            mountPath: /root/.mvnrepository
        EnvVars:
          JENKINS_URL: http://jenkins:8080
          JAVA_TOOL_OPTIONS: "-Dsun.zip.disableMemoryMapping=true -XX:+UseParallelGC -XX:MinHeapFreeRatio=5 -XX:MaxHeapFreeRatio=10 -XX:GCTimeRatio=4 -XX:AdaptiveSizePolicyWeight=90"
          GIT_COMMITTER_EMAIL: platform+jx-bot@nuxeo.com
          GIT_AUTHOR_EMAIL: platform+jx-bot@nuxeo.com
          GIT_AUTHOR_NAME: nuxeo-platform-jx-bot
          GIT_COMMITTER_NAME: nuxeo-platform-jx-bot
          XDG_CONFIG_HOME: /home/jenkins
          DOCKER_CONFIG: /home/jenkins/.docker/
          # lets disable the noisy download artifact logging messages
          MAVEN_OPTS: -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=warn
        ServiceAccount: jenkins
        ImagePullSecret: kubernetes-docker-cfg
        NodeSelector:
          team: platform
        Containers:
          Jnlp:
            Image: jenkinsci/jnlp-slave:3.26-1-alpine
            RequestCpu: "100m"
            RequestMemory: "128Mi"
            Args: "${computer.jnlpmac} ${computer.name}"
          Maven:
            Image: $DOCKER_REGISTRY/nuxeo/builder-java11-firefox42:latest
            Privileged: true
            RequestCpu: "3"
            RequestMemory: "4Gi"
            LimitCpu: "4"
            LimitMemory: "4Gi"
            Tty: true
          Maven-dev:
            Image: $DOCKER_REGISTRY/nuxeo/builder-java11-firefox42:latest
            Privileged: true
            RequestCpu: "4"
            RequestMemory: "4Gi"
            LimitCpu: "4"
            LimitMemory: "4Gi"
            Tty: true
          Maven-mongodb:
            Image: $DOCKER_REGISTRY/nuxeo/builder-java11-firefox42:latest
            Privileged: true
            RequestCpu: "4"
            RequestMemory: "4Gi"
            LimitCpu: "4"
            LimitMemory: "4Gi"
            Tty: true
          Maven-postgresql:
            Image: $DOCKER_REGISTRY/nuxeo/builder-java11-firefox42:latest
            Privileged: true
            RequestCpu: "4"
            RequestMemory: "4Gi"
            LimitCpu: "4"
            LimitMemory: "4Gi"
            Tty: true
      Nuxeo-Package-11:
        Name: nuxeo-package-11
        Label: jenkins-nuxeo-package-11
        DevPodPorts: 5005, 8080
        volumes:
          - type: Secret
            secretName: jenkins-maven-settings
            mountPath: /root/.m2/
          - type: Secret
            secretName: jenkins-docker-cfg
            mountPath: /home/jenkins/.docker
          - type: Secret
            secretName: jenkins-release-gpg
            mountPath: /home/jenkins/.gnupg
        EnvVars:
          JENKINS_URL: http://jenkins:8080
          JAVA_TOOL_OPTIONS: "-Dsun.zip.disableMemoryMapping=true -XX:+UseParallelGC -XX:MinHeapFreeRatio=5 -XX:MaxHeapFreeRatio=10 -XX:GCTimeRatio=4 -XX:AdaptiveSizePolicyWeight=90"
          GIT_COMMITTER_EMAIL: platform+jx-bot@nuxeo.com
          GIT_AUTHOR_EMAIL: platform+jx-bot@nuxeo.com
          GIT_AUTHOR_NAME: nuxeo-platform-jx-bot
          GIT_COMMITTER_NAME: nuxeo-platform-jx-bot
          XDG_CONFIG_HOME: /home/jenkins
          DOCKER_CONFIG: /home/jenkins/.docker/
          # lets disable the noisy download artifact logging messages
          MAVEN_OPTS: -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=warn
        ServiceAccount: jenkins
        ImagePullSecret: kubernetes-docker-cfg
        Containers:
          Jnlp:
            Image: jenkinsci/jnlp-slave:3.26-1-alpine
            RequestCpu: "100m"
            RequestMemory: "128Mi"
            Args: "${computer.jnlpmac} ${computer.name}"
          Maven:
            Image: $DOCKER_REGISTRY/nuxeo/builder-java11-firefox42:latest
            Privileged: true
            RequestCpu: "1"
            RequestMemory: "2Gi"
            LimitCpu: "2"
            LimitMemory: "3Gi"
            Tty: true
      Nuxeo-JSF-11:
        Name: nuxeo-jsf-11
        Label: jenkins-nuxeo-jsf-11
        DevPodPorts: 5005, 8080
        volumes:
          - type: Secret
            secretName: jenkins-maven-settings
            mountPath: /root/.m2/
          - type: Secret
            secretName: jenkins-docker-cfg
            mountPath: /home/jenkins/.docker
          - type: Secret
            secretName: jenkins-release-gpg
            mountPath: /home/jenkins/.gnupg
        EnvVars:
          JENKINS_URL: http://jenkins:8080
          JAVA_TOOL_OPTIONS: "-Dsun.zip.disableMemoryMapping=true -XX:+UseParallelGC -XX:MinHeapFreeRatio=5 -XX:MaxHeapFreeRatio=10 -XX:GCTimeRatio=4 -XX:AdaptiveSizePolicyWeight=90"
          GIT_COMMITTER_EMAIL: platform+jx-bot@nuxeo.com
          GIT_AUTHOR_EMAIL: platform+jx-bot@nuxeo.com
          GIT_AUTHOR_NAME: nuxeo-platform-jx-bot
          GIT_COMMITTER_NAME: nuxeo-platform-jx-bot
          XDG_CONFIG_HOME: /home/jenkins
          DOCKER_CONFIG: /home/jenkins/.docker/
          # lets disable the noisy download artifact logging messages
          MAVEN_OPTS: -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=warn
        ServiceAccount: jenkins
        ImagePullSecret: kubernetes-docker-cfg
        Containers:
          Jnlp:
            Image: jenkinsci/jnlp-slave:3.26-1-alpine
            RequestCpu: "100m"
            RequestMemory: "128Mi"
            Args: "${computer.jnlpmac} ${computer.name}"
          Maven:
            Image: $DOCKER_REGISTRY/nuxeo/builder-java11-firefox42-nodejs10:latest
            Privileged: true
            RequestCpu: "1.5"
            RequestMemory: "4Gi"
            LimitCpu: "3"
            LimitMemory: "6Gi"
            Tty: true
      Nuxeo-Third-Party-RPMs:
        Name: nuxeo-third-party-rpms
        Label: jenkins-nuxeo-third-party-rpms
        DevPodPorts: 5005
        volumes:
          - type: Secret
            secretName: jenkins-docker-cfg
            mountPath: /home/jenkins/.docker
          - type: Secret
            secretName: jenkins-release-gpg
            mountPath: /home/jenkins/.gnupg
        EnvVars:
          JENKINS_URL: http://jenkins:8080
          GIT_COMMITTER_EMAIL: platform+jx-bot@nuxeo.com
          GIT_AUTHOR_EMAIL: platform+jx-bot@nuxeo.com
          GIT_AUTHOR_NAME: nuxeo-platform-jx-bot
          GIT_COMMITTER_NAME: nuxeo-platform-jx-bot
          XDG_CONFIG_HOME: /home/jenkins
          DOCKER_CONFIG: /home/jenkins/.docker/
        ServiceAccount: jenkins
        ImagePullSecret: kubernetes-docker-cfg
        Containers:
          Jnlp:
            Image: jenkinsci/jnlp-slave:3.26-1-alpine
            RequestCpu: "100m"
            RequestMemory: "128Mi"
            Args: "${computer.jnlpmac} ${computer.name}"
          Rpm:
            Image: $DOCKER_REGISTRY/nuxeo/builder-rpm:latest
            Privileged: true
            RequestCpu: "250m"
            RequestMemory: "256Mi"
            Command: "/bin/sh -c"
            Args: "cat"
            Tty: true
docker-registry:
  enabled: false
nexus:
  persistence:
    size: 310Gi
    storageClass: standard
PipelineSecrets:
  DockerConfig: |-
    $DOCKER_REGISTRY_CONFIG
  MavenSettingsXML: |-
    <settings>
      <!-- sets the local maven repository outside of the ~/.m2 folder for easier mounting of secrets and repo -->
      <localRepository>${user.home}/.mvnrepository</localRepository>
      <!-- lets disable the download progress indicator that fills up logs -->
      <interactiveMode>false</interactiveMode>
      <mirrors>
        <mirror>
          <id>nexus</id>
          <mirrorOf>external:*</mirrorOf>
          <url>https://packages.nuxeo.com/repository/maven-internal/</url>
        </mirror>
      </mirrors>
      <servers>
        <server>
          <id>nexus</id>
          <username>deploymentJX</username>
          <password>$PACKAGES_PASSWORD</password>
        </server>
      </servers>
      <profiles>
        <profile>
          <id>nexus</id>
          <properties>
            <altDeploymentRepository>nexus::default::https://packages.nuxeo.com/repository/maven-team-platform/</altDeploymentRepository>
          </properties>
        </profile>
      </profiles>
      <activeProfiles>
        <activeProfile>nexus</activeProfile>
      </activeProfiles>
    </settings>
